extends layout

block content
    h1 Services

    each service, i in services
        .well
            h3 #{service.name}
            p #{service.description}
            p.
                Price Range: #{service.priceRange}